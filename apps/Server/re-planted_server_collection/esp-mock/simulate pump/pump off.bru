meta {
  name: pump off
  type: http
  seq: 1
}

post {
  url: http://localhost:{{sensor_mock}}/command/pump
  body: json
  auth: inherit
}

body:json {
  {
    "state": false
  }
}

script:post-response {
  if (res.getStatus() === 200) {
    bru.runner.setNextRequest("pump false");
  }
}

tests {
  test("geting res 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
