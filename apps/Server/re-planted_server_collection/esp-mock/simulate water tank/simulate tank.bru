meta {
  name: simulate tank
  type: http
  seq: 11
}

post {
  url: http://localhost:{{sensor_mock}}/simulate/water-tank
  body: json
  auth: inherit
}

body:json {
  {
    "levelCm": 5
  }
}

script:post-response {
  if (res.getStatus() === 200) {
    bru.runner.setNextRequest("check pump 5");
  }
}

tests {
  test("geting res 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
