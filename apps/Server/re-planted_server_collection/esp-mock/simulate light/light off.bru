meta {
  name: light off
  type: http
  seq: 5
}

post {
  url: http://localhost:{{sensor_mock}}/command/light
  body: json
  auth: inherit
}

body:json {
  {
    "state": false
  }
}

script:post-response {
  if (res.getStatus() === 200) {
    bru.runner.setNextRequest("light false");
  }
}

tests {
  test("geting res 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
