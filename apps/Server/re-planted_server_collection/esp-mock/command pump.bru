meta {
  name: command pump
  type: http
  seq: 2
}

post {
  url: http://localhost:{{sensor_mock}}/command/pump
  body: json
  auth: inherit
}

body:json {
  {
    "state": true
  }
}

tests {
  test("geting res 200", function () {
    expect(res.getStatus()).to.equal(200);
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
